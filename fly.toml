app = "daater-dashboard"
primary_region = "gru"

[build]

[env]
  PORT = "4321"
  HOST = "0.0.0.0"
  DB_FILE = "/data/daater.db"

[mounts]
  source = "daater_data"
  destination = "/data"

[http_service]
  internal_port = 4321
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
